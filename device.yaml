# device.yaml
network:
  listen_ip: "127.0.0.1"
  port: 20000

points:
  ai: 2        # tank_level, room_temp
  bo: 2        # fill_valve, drain_valve

process:
  dt_ms: 200
  tank:
    setpoint: 60.0
    fill_rate: 2.5
    drain_rate: 2.0
    leak_rate: 0.3
    noise: 0.2

visualization:
  enabled: true
